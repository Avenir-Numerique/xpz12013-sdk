# ReasonCode

A predefined set of reason code values + ability to extend this set

## Properties

Name | Type | Description | Notes
------------ | ------------- | ------------- | -------------

## Example

```python
from neotimo_xpz12013_flow.models.reason_code import ReasonCode

# TODO update the JSON string below
json = "{}"
# create an instance of ReasonCode from a JSON string
reason_code_instance = ReasonCode.from_json(json)
# print the JSON string representation of the object
print(ReasonCode.to_json())

# convert the object into a dict
reason_code_dict = reason_code_instance.to_dict()
# create an instance of ReasonCode from a dict
reason_code_from_dict = ReasonCode.from_dict(reason_code_dict)
```
[[Back to Model list]](../README.md#documentation-for-models) [[Back to API list]](../README.md#documentation-for-api-endpoints) [[Back to README]](../README.md)


